{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "types": ["cypress"] // be explicit about types included to avoid clashing with Jest types
  },
  "include": [
    "../node_modules/cypress",
    "./**/*.ts"
  ]
}
